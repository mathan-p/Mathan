CREATE TABLE EMP(
ID INT NOT NULL,
LASTNAME VARCHAR(20) NOT NULL,
FIRSTNAME VARCHAR(20),
AGE INT,
CONSTRAINT EMPLOYEE unique(ID,LASTNAME)
);

INSERT INTO EMP VALUES (2,'P','MATHAN', 22);

SELECT * FROM EMP;
